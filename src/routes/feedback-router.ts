import { Router } from 'express'

export const feedbackRouter = Router({})

// feedbackRouter

// .post('/', authMiddleware,
// async (req, res) => {
// const newItem = await feedbacksService.sendFeedback(req,body.comment, req.user!.id)
//     res.ststus(201).send(newItem)
// })
//
// .get('/', async (req.res) => {
// const users = await feedbacksService.allFeedbacks()
//
// res.send(users)
// })
