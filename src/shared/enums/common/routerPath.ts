export enum RouterPath {
  home = '/',
  auth = '/auth',
  blogs = '/blogs',
  posts = '/posts',
  users = '/users',
  comments = '/comments',
  security_devices = '/security/devices',
  testing = '/testing',
}
